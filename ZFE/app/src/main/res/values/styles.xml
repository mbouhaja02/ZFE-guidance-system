<resources>
    <style name="MyCustomButtonStyle" parent="@android:style/Widget.Material.Button">
        <!-- Couleur de fond -->
        <!-- <item name="android:background">@drawable/button_background</item> -->
        <item name="android:background">@drawable/button_background_selector</item>

        <item name="android:paddingLeft">12dp</item>
        <item name="android:paddingRight">12dp</item>
        <item name="android:paddingTop">6dp</item>
        <item name="android:paddingBottom">6dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:minWidth">48dp</item>
        <item name="android:minHeight">48dp</item>

        <!-- Couleur du texte -->
        <item name="android:textColor">@android:color/black</item>
        <!-- Tout autre attribut souhaité -->
    </style>
    <style name="ZfeAlertDialogTheme" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <!-- Personnalisez couleurs de fond, texte, etc. -->
        <item name="android:windowMinWidthMajor">@android:dimen/dialog_min_width_major</item>
        <item name="android:windowMinWidthMinor">@android:dimen/dialog_min_width_minor</item>
        <item name="android:colorAccent">@color/purple_500</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>
</resources>
